apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Release.Name }}
spec:
  ports:
  - name: metrics
    port: {{ .Values.port }}
  selector:
    app: {{ .Release.Name }}
